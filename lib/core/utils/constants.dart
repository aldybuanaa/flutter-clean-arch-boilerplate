/// Application-wide constants.
class AppConstants {
  AppConstants._();

  /// Default request timeout duration in seconds.
  static const int connectionTimeout = 30;

  /// Default receive timeout duration in seconds.
  static const int receiveTimeout = 30;
}
