import 'package:flutter_test/flutter_test.dart';

/// Smoke test is removed – the app no longer has a counter widget.
/// Feature-specific tests live in test/features/.
void main() {
  test('placeholder – see test/features/ for actual tests', () {
    expect(true, isTrue);
  });
}
